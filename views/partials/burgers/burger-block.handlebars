<li>
	{{name}}

	<button class="change-burger" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if burger}}devour{{else}}WAKE UP!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
